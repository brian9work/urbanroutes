eas build -p android --profile preview // Generate APK
npm install -g eas-cli // Install EAS CLI
eas login // Login to EAS
eas whoami // Check if logged in
eas build:configure // Configure EAS for the project
npx create-expo-app@latest aplication --template blank // Create new Expo app
git update-index --assume-unchanged file // Ignore changes in app.json
git update-index --no-assume-unchanged file // Stop ignoring changes in app.json


    